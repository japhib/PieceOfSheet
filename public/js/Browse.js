'use strict';

var React  = require('react');

var Browse = React.createClass({
    render: function() {
        return (
            <div>
            	Music to browse.
            </div>
            );
    }
});

module.exports = Browse;